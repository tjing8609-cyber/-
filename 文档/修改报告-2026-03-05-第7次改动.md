# 修改报告（2026-03-05 第7次改动）

## 步骤 1：GUI 支持配置 API 启动校验开关
- 状态：已完成
- 问题现象：虽然已支持 `verify_api_on_start` 配置，但 GUI 仍固定保存为 `false`。
- 原因判断：`auto_start.py` 未提供该字段的可视化配置入口，也未从已有配置回填。
- 已完成改动：
  - 在答题模式配置区新增“启动前验证 API 可用性”开关。
  - `load_config` 支持回填 `verify_api_on_start` 字段。
  - `save_config` 改为写入用户选择的 `verify_api_on_start`。
  - `create_new_config` 默认生成 `verify_api_on_start: false` 与 `course_url` 字段。
- 测试记录：
  - `python -m py_compile auto_start.py code/zhidao_quiz_only_player.py` 通过。
