# 修改报告（2026-03-05 第6次改动）

## 步骤 1：API 启动校验开关
- 状态：已完成
- 问题现象：纯答题模式启动即执行 API 连通性校验，会增加启动耗时与 token 成本。
- 原因判断：`zhidao_quiz_only_player.py` 在 `self.api_key` 存在时固定执行 `verify_api_connection()`。
- 已完成改动：
  - `zhidao_quiz_only_player.py` 增加 `verify_api_on_start` 开关，默认 `false` 时跳过启动连通性校验。
  - `auto_start.py` 保存配置时写入 `verify_api_on_start: false`，避免字段丢失。
  - `启动/account.json.template` 新增 `verify_api_on_start` 与说明字段。
- 测试记录：
  - `python -m py_compile auto_start.py code/zhidao_quiz_only_player.py` 通过。
