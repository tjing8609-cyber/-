# 修改报告（2026-03-05 第14次改动）

## 步骤 1：按路线图落地 3.1~3.6
- 状态：已完成
- 问题现象：建议功能已列入报告，但尚未形成工程化可执行能力。
- 原因判断：缺少统一治理入口，导致健康检查、编排、观测与恢复能力分散。
- 已完成改动：
  - 新增 `code/runtime_center.py`，统一提供配置健康检查、选择器加载、可观测状态写入、控制标志读取与失败快照保存。
  - `code/zhidao_launcher.py` 接入启动前健康检查与可执行性报告输出，运行状态写入 `runtime/observability.json`，失败时落盘快照。
  - 新增选择器热更新文件：`启动/selectors.default.json`、`启动/selectors.active.json`、`启动/selectors_versions/v1.json`。
  - `code/zhidao_web_auto_player_with_quiz.py` 与 `code/zhidao_web_auto_player_final.py` 接入外部选择器读取。
  - 新增版本切换工具 `tools/selector_profile_switch.py`，支持配置回退。
  - 新增任务编排中心 `code/task_orchestrator.py`，支持多账号队列、并发上限、失败重试与调度状态面板输出。
  - 新增安全暂停/恢复控制工具 `tools/runtime_control.py`，通过 `runtime/control.json` 控制调度暂停、恢复与停止。
  - 扩展 `tools/quality_check.py` 覆盖新增模块与选择器配置校验，并更新 README 使用说明。
- 测试记录：
  - `python tools/quality_check.py` 通过。
  - `python code/task_orchestrator.py --help` 通过。
