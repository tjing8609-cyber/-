# 修改报告（2026-03-05 第5次改动）

## 步骤 1：DeepSeek 环境变量优先级兼容
- 状态：已完成
- 问题现象：环境变量命名仍主要使用 `ANTHROPIC_*`，与 DeepSeek 语义不一致，易误配。
- 原因判断：纯答题模式与 GUI 读取系统配置时，都只读取 `ANTHROPIC_*`。
- 已完成改动：
  - `auto_start.py` 增加 `DEEPSEEK_*` 优先读取，并保留 `ANTHROPIC_*` 兼容兜底。
  - `zhidao_quiz_only_player.py` 增加 `DEEPSEEK_*` 优先读取，并更新 401 错误指引文案。
  - `启动/account.json.template` 更新环境变量说明为 DeepSeek 优先策略。
- 测试记录：
  - `python -m py_compile auto_start.py code/zhidao_quiz_only_player.py` 通过。
