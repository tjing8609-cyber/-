================================================================
知到自动化脚本 - 当前方法与潜在检测点完整分析
================================================================
生成时间：2025-11-20
脚本版本：v3.8（有题目课程版本）
================================================================

本文档详细列出脚本中所有自动化操作的实现方法、使用的技术手段、
以及可能被反脚本机制检测的风险点，供优化改进参考。

================================================================
一、登录处理部分
================================================================

【1.1 登录方法】
文件位置：zhidao_web_auto_player_with_quiz.py - login()方法

实现流程：
1. 访问登录页面（固定URL）
2. 查找用户名输入框（XPath选择器）
3. 查找密码输入框（XPath选择器）
4. 查找登录按钮（XPath选择器）
5. 填写账号密码
6. 点击登录
7. 检测人机验证
8. 等待登录成功

选择器列表：
- 用户名框：//input[@type='text']
- 密码框：//input[@type='password']
- 登录按钮：//span[contains(text(), '登录')]

🚨 潜在检测点：
❌ 输入速度过快 - send_keys()瞬间完成
❌ 没有模拟焦点切换 - 直接输入，不点击输入框
❌ 登录URL固定 - 总是从同一个URL登录
❌ 固定的查找顺序 - 每次都按相同顺序查找元素
❌ 检查间隔固定 - 每2秒检查一次登录状态

【1.2 人机验证处理】
实现方法：
- 检测验证码弹窗
- 等待用户手动完成
- 每2秒检查一次是否完成
- 检测到登录成功后立即继续

🚨 潜在检测点：
❌ 检查间隔固定 - 精确2秒
❌ 响应过快 - 验证通过后立即继续（没有人工反应时间）

================================================================
二、课程查找部分
================================================================

【2.1 课程查找方法】
文件位置：zhidao_web_auto_player_with_quiz.py - find_course()方法

实现流程：
1. 检测页面布局版本（新版/旧版）
2. 查找"共享课"标签
3. 点击激活标签
4. 滚动加载课程列表
5. 查找目标课程卡片
6. 点击进入课程

课程卡片选择器（9个）：
- //div[contains(@class, 'course-card')]
- //div[contains(@class, 'courseCardItem')]
- //div[contains(@class, 'clearfix')]
- //li[contains(@class, 'course-list-item')]
- //div[contains(@class, 'course-info')]
- //a[contains(@class, 'course-item')]
- //div[@role='listitem']
- //div[contains(@class, 'el-card')]
- //div[contains(@class, 'class-panel')]

滚动加载机制：
- 最多滚动20次
- 每次滚动500px
- 每次等待1秒

🚨 潜在检测点：
❌ 选择器数量过多 - 9个选择器按顺序尝试
❌ 滚动参数固定 - 总是500px，总是1秒
❌ 滚动次数固定 - 最多20次
❌ 课程名称精确匹配 - 使用配置文件中的课程名
❌ 点击后无延迟 - 找到课程立即点击

【2.2 课程进度识别】
实现方法：
- 提取进度文本（正则表达式）
- 匹配格式：XX.X% 或 XX%

🚨 潜在检测点：
⚠️ 正则表达式固定 - r'(\d+\.?\d*)%'

================================================================
三、学习页面进入
================================================================

【3.1 进入学习页面】
文件位置：zhidao_web_auto_player_with_quiz.py - enter_study_page()方法

实现流程：
1. 等待页面加载（5秒）
2. 查找"继续学习"按钮
3. 点击按钮
4. 关闭弹窗

继续学习按钮选择器（6个）：
- //button[contains(text(), '继续学习')]
- //a[contains(text(), '继续学习')]
- //span[contains(text(), '继续学习')]
- //div[contains(text(), '继续学习')]
- //*[contains(text(), '开始学习')]
- //*[contains(text(), '进入学习')]

🚨 潜在检测点：
❌ 等待时间固定 - 总是5秒
❌ 选择器数量多 - 6个按顺序尝试
❌ 立即点击 - 找到按钮后立即点击

【3.2 弹窗关闭方法】
文件位置：zhidao_web_auto_player_with_quiz.py - close_all_popups()方法

查找的弹窗类型（10+种）：
- el-icon-close（Element UI关闭图标）
- popTopicChoose（题目弹窗）
- dialogClose（对话框关闭）
- 学前必读
- AI助教
- 公告
- 通知
- 温馨提示
- 活动
- 广告

关闭方式：
1. JavaScript执行点击
2. 普通click()
3. ActionChains点击

🚨 潜在检测点：
❌ 关闭速度过快 - 找到弹窗立即关闭
❌ 关闭所有弹窗 - 人工可能会看一眼
❌ JavaScript点击优先 - 容易被检测

================================================================
四、视频列表查找
================================================================

【4.1 视频元素查找】
文件位置：zhidao_web_auto_player_with_quiz.py - find_unwatched_videos()方法

实现流程：
1. 查找右侧目录容器
2. 滚动加载全部视频
3. 使用多选择器查找视频元素
4. 去重处理
5. 过滤已观看视频

视频元素选择器（3个）：
- .//li[contains(@class, 'clearfix')]
- .//li[contains(@class, 'video') or contains(@class, 'lesson')]
- .//li

滚动机制：
- 鼠标滚轮模拟滚动
- 每次滚动500px
- 最多滚动20次
- 检测3次位置不变则停止

🚨 潜在检测点：
❌ 滚动参数固定 - deltaY=500
❌ 滚动速度固定 - 每次间隔固定
❌ 选择器组合固定 - 总是这3个选择器
❌ 去重逻辑可预测 - 基于元素引用

【4.2 完成状态判断】
判断方法（3种）：
1. 查找完成标记class（time_icofinish）
2. 检查进度是否100%
3. 文本包含"100%"、"已完成"

🚨 潜在检测点：
⚠️ 判断逻辑固定
⚠️ 选择器固定

================================================================
五、视频播放控制
================================================================

【5.1 视频启动方法】
实现方式：
1. 点击视频元素
2. 等待3秒加载
3. JavaScript执行video.play()

JavaScript代码：
```javascript
var video = document.querySelector('video');
if (video) {
    video.play();
}
```

🚨 潜在检测点：
❌ 等待时间固定 - 3秒
❌ JavaScript直接操作video - 不触发UI事件
❌ 没有用户交互 - play()可能被浏览器阻止

【5.2 视频播放监控】
文件位置：wait_for_video_complete()方法

监控机制：
- 检查间隔：固定10秒
- 检查项：题目弹窗、视频播放状态、进度变化
- 卡停检测：连续3次进度无变化

🚨 潜在检测点：
❌ 检查间隔固定 - 精确10秒
❌ 检查次数固定 - 总是3次无变化触发
❌ 响应太快 - 检测到问题立即处理

【5.3 视频恢复机制】
文件位置：recover_stuck_video()方法

恢复步骤：
1. 点击视频中央区域
2. 执行video.pause()
3. 等待500ms
4. 执行video.play()

🚨 潜在检测点：
❌ 点击位置固定 - 总是中央点
❌ 暂停时间固定 - 总是500ms
❌ JavaScript操作 - 不触发真实事件

================================================================
六、题目处理部分（重点）
================================================================

【6.1 题目检测方法】
文件位置：check_for_quiz()方法

检测流程：
1. 先排除AI助手弹窗
2. 使用6个选择器查找题目元素

题目选择器：
- //div[contains(@class, 'topic-item')]
- //div[contains(@class, 'subject-item')]
- //div[contains(@class, 'popTopicChoose')]
- //*[contains(text(), 'AI助教') or contains(text(), '出题')]
- //*[contains(text(), '题目') or contains(text(), '选择')]
- //div[contains(@class, 'el-dialog')]//input[@type='radio']

🚨 潜在检测点：
❌ 选择器列表固定 - 总是这6个
❌ 查找顺序固定 - 按列表顺序
❌ 检查间隔固定 - 在监控循环中每10秒检查
❌ 立即响应 - 检测到题目立即回答

【6.2 选项查找方法】
文件位置：answer_quiz()方法

选项选择器（13个）：
1. //input[@type='radio']
2. //input[@type='checkbox']
3. //label[contains(@class, 'el-radio')]
4. //label[contains(@class, 'el-checkbox')]
5. //div[contains(@class, 'el-radio')]
6. //div[contains(@class, 'el-checkbox')]
7. //span[contains(@class, 'el-radio__label')]
8. //span[contains(@class, 'el-checkbox__label')]
9. //li[contains(@class, 'topic-item')]
10. //div[contains(@class, 'topic-option-item')]
11. //div[contains(@class, 'option-item')]
12. //div[contains(@class, 'answer-option')]
13. //span[contains(text(), 'A') or contains(text(), 'B')...]/ancestor::li

🚨 潜在检测点：
❌ 选择器数量过多 - 13个选择器
❌ 查找顺序固定 - 总是按这个顺序
❌ 找到即停止 - break逻辑很机械
❌ 没有阅读题目时间 - 立即开始选择

【6.3 选项点击方法】

当前实现（已优化）：
1. 随机延时0-3秒
2. ActionChains移动到元素
3. 暂停0.3秒
4. 点击

降级策略：
ActionChains → 普通click() → JavaScript点击

🚨 潜在检测点：
⚠️ ActionChains轨迹 - 直线移动，不是曲线
⚠️ pause时间固定 - 总是0.3秒
❌ 没有思考时间 - 看到题目立即选择
❌ 选项顺序固定 - 总是A→B→C→D

【6.4 提交按钮点击】

当前实现（已优化）：
1. 随机延时1-3秒（选择后到提交）
2. 查找5种提交按钮选择器
3. ActionChains点击

提交按钮选择器：
- //span[contains(text(), '确定')]
- //button[contains(text(), '确定')]
- //span[contains(text(), '提交')]
- //button[contains(text(), '提交')]
- //div[contains(@class, 'popbtn_ok')]

🚨 潜在检测点：
❌ 提交太快 - 从选择到提交只有1-3秒
❌ 没有检查题目 - 人工会先看题干
❌ 查找顺序固定
⚠️ 延时范围窄 - 1-3秒，人工需要3-8秒

【6.5 答案结果识别】
文件位置：check_answer_result()方法

识别方法：
1. 优先查找错误标记（class='error'）
2. 查找正确标记（class='right'）
3. 文本匹配"正确"、"错误"

选择器：
错误标记：
- //*[contains(@class, 'error')]
- //*[contains(@class, 'wrong')]
- //*[contains(text(), '错误') or contains(text(), '不正确')]

正确标记：
- //*[contains(@class, 'right')]
- //*[contains(@class, 'correct')]
- //*[contains(@class, 'success')]
- //*[contains(text(), '正确') or contains(text(), '恭喜')]

🚨 潜在检测点：
⚠️ 识别逻辑固定
⚠️ 查找顺序固定

【6.6 正确答案提取】
文件位置：extract_correct_answer()方法

提取方法：
- 正则表达式：r'正确答案[：:]\s*([A-D])'
- 查找包含"正确答案"的元素

🚨 潜在检测点：
⚠️ 正则表达式固定
⚠️ 没有模拟查看答案的时间

【6.7 点击正确答案】

当前实现（已优化）：
1. 随机延时0.5-2秒
2. ActionChains移动点击
3. 降级策略

🚨 潜在检测点：
❌ 答错后重试太快 - 0.5-2秒就重新选择
❌ 没有阅读答案解析时间

【6.8 关闭题目弹窗】
文件位置：close_quiz_dialog()方法

当前实现（已优化）：
1. 随机延时0.5-1.5秒
2. 12个关闭按钮选择器
3. ActionChains点击

关闭按钮选择器：
1. //span[contains(@class, 'dialog-footer')]//div...text()='关闭'
2. //div[@class='el-dialog__footer']//span[@class='dialog-footer']//div[@class='btn']
3. //span[@class='dialog-footer']//div[@class='btn']
4. //div[contains(@class, 'el-dialog__footer')]//div...contains(text(), '关闭')
5. //button[contains(@class, 'el-dialog__headerbtn')]
6. //button[contains(@class, 'el-dialog__close')]
7. //i[contains(@class, 'el-icon-close')]
8. //button[contains(text(), '关闭')]
9. //div[contains(@class, 'el-icon-close')]
10. //i[contains(@class, 'close')]
11. //*[@title='关闭']
12. //button[contains(@class, 'close')]

🚨 潜在检测点：
❌ 关闭太快 - 看到结果0.5-1.5秒就关闭
❌ 没有查看结果时间 - 人工需要2-3秒
❌ 选择器数量过多 - 12个选择器
❌ 查找顺序固定

【6.9 多选题处理】

处理逻辑：
1. 检测checkbox类型
2. 选择所有ABCD选项
3. 每个选项随机延时0-3秒
4. ActionChains点击

🚨 潜在检测点：
❌ 全选策略可预测
❌ 选择顺序固定 - A→B→C→D
⚠️ 每个选项都延时 - 但总时间仍可预测

================================================================
七、视频恢复播放
================================================================

【7.1 答题后恢复播放】
文件位置：answer_quiz()方法中的视频恢复部分

实现方法：
```javascript
var video = document.querySelector('video');
if (video) {
    // 点击视频中央
    var rect = video.getBoundingClientRect();
    var centerX = rect.left + rect.width / 2;
    var centerY = rect.top + rect.height / 2;
    var clickEvent = new MouseEvent('click', {
        view: window,
        bubbles: true,
        cancelable: true,
        clientX: centerX,
        clientY: centerY
    });
    video.dispatchEvent(clickEvent);
    video.play();
}
```

🚨 潜在检测点：
❌ 点击位置固定 - 总是正中央
❌ JavaScript创建事件 - 可被检测
❌ 立即执行 - 关闭弹窗后立即播放

================================================================
八、其他辅助功能
================================================================

【8.1 智能等待 smart_wait()】
实现：
```python
time.sleep(seconds)
```

🚨 潜在检测点：
❌ 使用time.sleep - 完全阻塞，不自然
⚠️ 应改用随机波动的等待

【8.2 调试信息保存】
保存内容：
- HTML页面源码
- 截图
- 日志文件

保存时机：
- 找不到元素时
- 操作失败时

🚨 潜在检测点：
⚠️ 频繁的DOM访问（page_source）

【8.3 进度保存机制】
保存内容：
- 已完成视频列表
- 答题统计
- 观看时长

保存时机：
- 每个视频完成后
- 每次答题后

================================================================
九、检测风险评级
================================================================

【🔴 高风险（必须改进）】
1. ❌ 题目检测间隔固定（10秒）
2. ❌ 选项尝试顺序固定（A→B→C→D）
3. ❌ 选择器数量过多且顺序固定
4. ❌ 答题总时长过短（3-6秒）
5. ❌ 没有阅读题目时间
6. ❌ 关闭弹窗太快（0.5-1.5秒）
7. ❌ JavaScript直接操作video元素

【🟡 中风险（建议改进）】
1. ⚠️ ActionChains直线移动轨迹
2. ⚠️ pause时间固定（0.3秒）
3. ⚠️ 提交延时范围窄（1-3秒）
4. ⚠️ 滚动参数固定
5. ⚠️ 点击位置固定（视频中央）

【🟢 低风险（可选优化）】
1. ✓ 随机延时已实现（但范围可扩大）
2. ✓ ActionChains已使用
3. ✓ 三层降级策略

================================================================
十、改进优先级建议
================================================================

【P0 - 必须立即改进】
1. 添加"阅读题目"时间（5-10秒）
2. 打乱选项尝试顺序（不要固定A→B→C→D）
3. 增加答题总时长（至少10-15秒）
4. 答错后查看答案时间（2-3秒）
5. 看到结果后延长关闭时间（2-4秒）

【P1 - 重要改进】
6. 监控间隔随机化（8-12秒）
7. 减少选择器数量（只保留2-3个最常用）
8. 提交延时扩大范围（2-5秒）
9. 滚动参数随机化
10. 添加鼠标移动轨迹曲线

【P2 - 增强优化】
11. 模拟焦点切换（答题前看题干）
12. 输入速度模拟（登录时）
13. 页面停留时间随机化
14. 添加偶尔的"失误"操作（点错后重新点）

================================================================
十一、webdriver检测特征
================================================================

【浏览器指纹检测】
当前状态：未处理

可能的检测点：
- navigator.webdriver = true
- window.chrome缺失某些属性
- navigator.plugins为空或异常
- navigator.languages异常

【建议】：
- 使用undetected-chromedriver
- 或添加CDP命令隐藏webdriver特征

【自动化特征】
- 缺少用户交互事件
- 事件触发顺序异常
- 鼠标轨迹不自然
- 时间间隔规律

================================================================
十二、总体评估
================================================================

当前脚本的自动化特征明显度：★★★★☆（4/5）

主要问题：
1. 时间间隔过于规律
2. 操作顺序固定可预测
3. 答题速度不符合人工习惯
4. 缺少思考和阅读时间
5. 选择器查找模式固定

改进后可降低到：★★☆☆☆（2/5）

================================================================
文档结束
================================================================
